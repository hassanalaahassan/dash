export interface Insight {
     id: string
     content: string
     frontMatter: FrontMatter

 }
export interface FrontMatter {
     date: string
     images: string
     category: any
     summary: string
     title: string

  }
